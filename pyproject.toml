[project]
name = "kyrgyz-llm-benchmark"
version = "0.1.0"
description = "A native-speaker-authored benchmark measuring LLM competence in Kyrgyz."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
